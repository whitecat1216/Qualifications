# Python3 資格試験 第2回 まとめ＆サルでもわかる解説

> 各問を「答え」と「なぜそうなるか」に分けて、初心者向けに解説しています。

---

## 目次
1. [Pythonの特徴・インタープリタ](#1-pythonの特徴インタープリタ)
2. [数値・演算・文字列](#2-数値演算文字列)
3. [制御構文・ループ](#3-制御構文ループ)
4. [リスト・データ構造](#4-リストデータ構造)
5. [関数・スコープ・引数](#5-関数スコープ引数)
6. [モジュール・標準ライブラリ](#6-モジュール標準ライブラリ)
7. [例外・ファイル・クラス](#7-例外ファイルクラス)
8. [その他（正規表現・pip・対話環境）](#8-その他)

---

## 1. Pythonの特徴・インタープリタ

### 問1：Pythonの特徴で**誤っている**ものはどれか

**答え：**  
「データ型の一般性が高いためPythonの対応可能な問題領域はAwkより広いが、**Perlと比べると同程度である**」が誤り。

**サルでもわかる解説：**  
- 正しくは「**Perlより広い**」です。  
- Pythonは「書きやすさ」と「できることの広さ」の両方で、Perlより対応できる分野が広いとされています。  
- 「Awkより広い」は正しいので、ひっかけは「Perlと同程度」の部分だけ。

---

### 問2：Pythonインタープリタで**誤っている**ものはどれか

**答え：**  
「**import listitems** を実行することで、このリストにアクセスできる」が誤り。

**サルでもわかる解説：**  
- コマンドライン引数（スクリプト名やその後の引数）にアクセスするのは **`sys.argv`** です。  
- そのために **`import sys`** をして、`sys.argv` というリストを使います。  
- **`listitems` というモジュールは存在しません**。名前が似ているだけのひっかけ。

---

## 2. 数値・演算・文字列

### 問3：数値に関する記述で**正しい**ものはどれか

**答え：**  
「変数は、定義（値の代入）や宣言がなされないまま使おうとするとエラーとなる。」

**サルでもわかる解説：**  
- **オペランド**＝演算の**対象**（数など）、**演算子**＝「+」や「-」などの**記号**。問題文は逆になっているので誤り。  
- **切り下げ除算**（商だけ欲しい）は **`//`**、**剰余**は **`%`**。`/` と `//` が逆の説明なので誤り。  
- 除算 **`/`** は Python 3 では常に float を返すが、「除算は常に float」だけだと **`//`** を忘れているので不正確。  
- 最後の式の結果が入る変数は **`_`**（アンダースコア1つ）。`**` ではない。  
- よって、唯一正しいのは「未定義の変数を使うとエラー」です。

---

### 問4：次のコードの実行結果

```python
a = 2
b = a ** 3    # 2の3乗 = 8
c = b / 2 + 3 # 8/2+3 = 4+3 = 7
d = 10
e = d // b    # 10//8 = 1
f = d % c     # 10%7 = 3
print('{1}, {0}'.format(e, f))  # {0}=e=1, {1}=f=3 → "3, 1"
```

**答え：** **3.0, 1**（※ `f` は整数3だが、formatの結果として「3」と表示され、選択肢では 3.0, 1 と書かれているもの）

**サルでもわかる解説：**  
- `format(e, f)` では **0番目＝e、1番目＝f**。  
- `'{1}, {0}'` は「**先に f、次に e**」を表示するので「3, 1」です。  
- 選択肢の「3.0, 1」は、表示が「3」と「1」であることを表しています。

---

### 問5：文字列で**正しい**ものはどれか

**答え：**  
「対話型インタープリタでは文字列は引用符に囲まれ、特殊文字はバックスラッシュでエスケープされた状態で出力される。**print()関数では全体を囲む引用符が除去され、エスケープ文字や特殊文字が解釈された状態で出力される。**」

**サルでもわかる解説：**  
- **raw 文字列**は先頭に **`r`** を付ける（例：`r'C:\some\name'`）。`raw` という単語は使わない。  
- 複数行で「行末を文字列に含めたくない」ときは行末に **`\`** を置く。「-」ではない。  
- 文字列の連結は **`+`**。`.` はメソッド呼び出し（例：`'a'.upper()`）用。  
- `'w' + 3*'o'` は **`'wooo'`**（wが1つ、oが3つ）。「wwwo」ではない。  
- 対話モードでは `repr` 風（引用符付き・エスケープ見える）、`print()` では「人が読む形」で出力、という説明が正しいです。

---

### 問6：実行結果が `sNow` になる【A】は？

`Zen = 'NowIsBetterThanNever'` で `print('{}{}{}'.format(【A】))` → `sNow`

**答え：** **Zen[4], Zen[-5], Zen[1:3]**

**サルでもわかる解説：**  
- インデックスは **0始まり**。  
- **Zen[4]** = 5文字目 = **'s'**（Now**I**s の s）  
- **Zen[-5]** = 後ろから5番目 = **'N'**（Ne**v**er の前の N → Never の N）  
  - 実際：-1=r, -2=e, -3=e, -4=V, -5=**N**  
- **Zen[1:3]** = 1番目～2番目 = **'oW'**（Now の o と W）  
- つなげると **s + N + oW = "sNow"** になります。

---

### 問7：エラーにな**らない**ものはどれか

`Zen = 'BeautifulIsBetterThanUgly'`

**答え：** **Zen[1000:10000]**

**サルでもわかる解説：**  
- **スライス**（`[始まり:終わり]`）は、範囲外でも**空文字列 `''` を返すだけ**でエラーになりません。  
- **Zen[50]** → インデックスが範囲外なので **IndexError**。  
- **Zen[10] = 'a'** → 文字列は**イミュータブル**（書き換え不可）なので **TypeError**。  
- **Zen['B']** → インデックスは**整数**しか使えないので **TypeError**。  
- **Zen[1:10] + b** → `b` が未定義なので **NameError**。

---

## 3. 制御構文・ループ

### 問8：while ループの実行結果（フィボナッチ風）

```python
a, b = 0, 1
while a < 10:
    print(a, end=',')
    a, b = b, a+b
```

**答え：** **0,1,1,2,3,5,8,**

**サルでもわかる解説：**  
- 最初に **a=0** を表示してから **a, b = 1, 1**。  
- 次に **a=1** を表示して **a, b = 1, 2**。  
- その次 **a=2** → **a, b = 2, 3** … と、フィボナッチ数列になる。  
- **a < 10** の間だけ回るので、最後に表示されるのは **8** まで。  
- 表示は「0,1,1,2,3,5,8,」です。

---

### 問9：リストの append / insert の結果

**答え：** **['September', 'January', 'January', 'March', 'May', 'July', 'September']**

**サルでもわかる解説：**  
- `months.append('September')` で末尾に追加。  
- `for month in months[:]` で**コピー**を回しているので、ループ中にリストを変えてもループ順は変わらない。  
- **len(month) > 5** のときだけ `months.insert(0, month)` で**先頭に**挿入。  
- 6文字より長いのは **January, July, September**。この順で先頭に挿入されていくので、上記の並びになります。

---

### 問10：素数判定の【A】【B】

**答え：** **【A】break　【B】else:**

**サルでもわかる解説：**  
- **n % x == 0** のときは「n = x * (n//x)」と表示して、**その n の判定を終える**ので **break**。  
- **for** ループの **else** は「**break で抜けなかったときだけ**」実行される。  
- つまり「どの x でも割り切れなかったとき」＝素数なので、**【B】は else:** で「素数」を表示。  
- 「each:」や「else」だけ（コロンなし）は文法エラーなので ×。

---

### 問11：range(-3, -18, -3) の結果

**答え：** **-3, -6, -9, -12, -15,**

**サルでもわかる解説：**  
- **range(始まり, 終わり, ステップ)** で、**終わりは含まない**。  
- -3 から -3 ずつ進めて、**-18 の手前**（-15）まで。  
- だから -3, -6, -9, -12, -15 の5個。**-18 は出てこない**。

---

### 問12：enumerate の代わりになる書き方

`for i, v in enumerate(Zen):` の代わりで、同じ結果になるもの。

**答え：** **for i in range(len(Zen)):**（その次の行に `print(i, Zen[i])` がある想定）

**サルでもわかる解説：**  
- `enumerate(Zen)` は「(0, 先頭の要素), (1, 2番目), …」を返す。  
- 同じことをするには **i を 0 ～ len(Zen)-1** で回して、**Zen[i]** を使えばよい。  
- つまり **for i in range(len(Zen)):** が正解。  
- 「in i for」「while i < range(...)」などは文法として不正なので ×。

---

## 4. リスト・データ構造

### 問13：グローバル変数 i と関数 f(arg) の出力

**答え：** **5**

**サルでもわかる解説：**  
- グローバルで **i** が何度か代入され、最後が **i = 5**。  
- **f(i)** は **f(5)** なので、**arg = 5**。関数内の `i = 3` は**ローカル**の i で、**arg** には影響しない。  
- **print(arg)** なので **5** が出力されます。

---

### 問14：デフォルト引数がリストのときの挙動

**答え：** **【A】の行（3回目の print(culc(4,4))）の出力は ([4, 9, 16], [8, 27, 64])**

**サルでもわかる解説：**  
- **デフォルト引数**の **squares=[]**, **cubes=[]** は、**関数定義時に1回だけ**作られたリストをずっと使い回します。  
- 1回目: squares=[4], cubes=[8]  
- 2回目: squares=[4,9], cubes=[8,27]  
- 3回目: squares=[4,9,16], cubes=[8,27,64]  
- だから **print(culc(4,4))** では **([4, 9, 16], [8, 27, 64])** が正解。  
- デフォルトでは**可変オブジェクト（リスト）をデフォルト引数にしない**のが鉄則です。

---

### 問15：キーワード引数で正しい呼び出し

`def location(city, state='NewYork', country='USA'):`

**答え：** **location(state='Jakarta', city='Cikini')**

**サルでもわかる解説：**  
- **city** は必須（デフォルトなし）。**state** と **country** は省略可能。  
- 正解では **city** と **state** がキーワードで指定されており、**country** は省略なのでデフォルトの 'USA' が使われる。  
- 他は「必須の city がない」「位置引数がキーワード引数の後にある」などでエラーになります。

---

### 問16：*args と **kwargs の【A】【B】

**答え：** **【A】*argsY　【B】**argsX**

**サルでもわかる解説：**  
- ***argsY** = 余った**位置引数**がタプルで入る。  
- **\*\*argsX** = **キーワード引数**が辞書で入る。  
- コード内で **argsX.keys()** を使っているので、**argsX が辞書**でないといけない → **argsX は \*\* で受け取る**。  
- だから **【A】*argsY　【B】**argsX** が正解。

---

### 問17：記述で**誤っている**もの（関数注釈・docstring・PEP8）

**答え：**  
「例えば『def func(a: int, b: str) -> value』と関数を記述したときにアノテーションに該当するものは**『-> value』のみ**である」が誤り。

**サルでもわかる解説：**  
- **アノテーション**は **引数の `: 型`** と **戻り値の `-> 型`** の**両方**を指します。  
- なので「-> value のみ」という説明は誤り。  
- 他の選択肢（__annotations__ に格納、docstring の書き方、PEP 8 のスペース）は正しい説明です。

---

### 問18：リスト内包で [0, 9, 36, 81] を作る

**答え：** **squares = [x ** 2 for x in range(0, 10, 3)]**

**サルでもわかる解説：**  
- **range(0, 10, 3)** → 0, 3, 6, 9。  
- それぞれを2乗すると 0, 9, 36, 81。  
- リスト内包の形は **【結果 for 変数 in イテラブル】** なので、**x ** 2 for x in range(...)** が正解。  
- 「x in x ** 2」などは順序が逆で誤り。

---

### 問19：行列の転置（行と列を入れ替える）

**答え：** **power = list(zip(*matrix))**

**サルでもわかる解説：**  
- **\*matrix** でリストのリストを「1つずつ引数として」渡す。  
- **zip(リスト1, リスト2, リスト3)** は「1番目同士、2番目同士、3番目同士」をタプルにする。  
- つまり **zip(*matrix)** で「列」をまとめたタプルが得られ、**転置**になる。  
- **list(zip(...))** でリストのリスト（またはタプルのリスト）にすれば、元の ★ の行と同じ結果になります。

---

### 問20：リスト内包で (x, y) の組み合わせ

**答え：** **combs = [(a, b) for a in [1,2,3] for b in [3,2,5] if a != b]**

**サルでもわかる解説：**  
- 「a を [1,2,3]、b を [3,2,5] で回して、a ≠ b のとき (a, b) を入れる」がそのままの形。  
- **for a in ... for b in ...** の順と、**if a != b** を付ける形だけ覚えればOK。  
- **[a,b]** だとリストになり、**(a,b)** がタプル。問題の結果はタプルのリストなので **(a,b)**。  
- **a = b** は代入なので比較には使えず、**a != b** が正しい。

---

### 問21：リストの append / sort / insert / pop の結果

**答え：** ** [50, 30, 5, 20, 15, 1]**

**サルでもわかる解説：**  
- 初期: [-10, 1, 15, 20, 30]  
- **append(50)** → [-10, 1, 15, 20, 30, 50]  
- **sort(reverse=True)** → [50, 30, 20, 15, 1, -10]（大きい順）  
- **insert(2, 5)** → インデックス2の位置に5を挿入 → [50, 30, **5**, 20, 15, 1, -10]  
- **pop(-1)** → 最後（-10）を削除 → [50, 30, 5, 20, 15, 1]  
- ※ 組み込み名 **list** を変数名に使うのは避けましょう。

---

### 問22：スライス Zen[0:20:3]

`Zen = 'FlatIsBetterThanNested'`

**答え：** 0, 3, 6, 9, 12, 15, 18 番目の文字を取るので、**FtBtTns** など（問題の選択肢に合わせた表記）。

**サルでもわかる解説：**  
- **[始まり:終わり:ステップ]** で、**終わりは含まない**。  
- 0, 3, 6, 9, 12, 15, 18 番目を連結した文字列が結果です。  
- 実際に手で数えるか、対話モードで `Zen[0:20:3]` を実行すると確実です。

---

### 問23：データ構造で**正しい**ものはどれか

**答え：**  
「リストとタプルは**順序を持つ**要素の集まりであるという共通点がある。」

**サルでもわかる解説：**  
- **in / not in** の対象は**キー**（値ではない）。  
- **{}** は**空の辞書**。空集合は **set()** で作る。  
- **タプルはイミュータブル**。**集合（set）はミュータブル**。  
- **辞書はミュータブル**。キーはイミュータブルでないとダメな場合が多い。  
- リストもタプルも「何番目が何」が決まっている＝順序あり、という説明が正しいです。

---

### 問24：対話モードで True が返るもの

**答え：** **1 > -1 == (1-2)**

**サルでもわかる解説：**  
- 比較が**連鎖**すると、**隣同士が両方 True のときだけ**全体が True になる。  
- **1 > -1** → True。**(1-2) = -1** なので **-1 == (1-2)** → True。  
- だから **1 > -1 == (1-2)** は **True and True** で **True**。  
- その他の選択肢：  
  - **(1, 2) > (1, 2, -1)** → タプルは先頭から比較し、同じなら長い方が大きい。なので (1,2) < (1,2,-1) で **False**。  
  - **(-1, -10, -3, -4) > (-1, -2, -5)** → 2番目 -10 と -2 で -10 < -2 なので **False**。  
- 「比較の連鎖」のルールを押さえておきましょう。

---

## 5. 関数・スコープ・引数

（問13〜17は上にまとめたので、ここでは問18〜20をリスト内包として扱いました。関数周りは問14〜17を参照。）

---

## 6. モジュール・標準ライブラリ

### 問25：モジュールで**誤っている**ものはどれか

**答え：**  
「**sys.path が初期化されている場所は、入力スクリプトのあるディレクトリ、PYTHONPATH であり、インストールごとのデフォルトは含まれない**」が誤り。

**サルでもわかる解説：**  
- **sys.path** には、**インストール時に決まるデフォルトのディレクトリも含まれます**。  
- なので「デフォルトは含まれない」という部分が誤り。  
- パッケージの説明、検索順（ビルトイン→sys.path）、.pyc の自動再コンパイル、コンパイル済みのプラットフォーム依存性の説明は正しいです。

---

### 問26：モジュールの名前を確認する【A】

**答え：** **dir(sys)**

**サルでもわかる解説：**  
- **dir(オブジェクト)** で、そのオブジェクトが持っている名前（属性・メソッドなど）のリストが得られます。  
- **mod()** や **mod(sys)** のような関数は標準にはありません。  
- だから **【A】= dir(sys)** が正解。

---

### 問27：format と math

**答え：** **2.71828, 3.142**（または「2.71828, 3.142」と表示される選択肢）

**サルでもわかる解説：**  
- **format(math.pi, math.e)** では **{0}=pi, {1}=e**。  
- **'{1:.5f}, {0:.3f}'** は「**1番目を小数点5桁、0番目を小数点3桁**」なので、**e を .5f、pi を .3f**。  
- つまり **2.71828, 3.142** の順で表示されます。

---

## 7. 例外・ファイル・クラス

### 問28：try / except（整数 a=3, b=0 を入力）

**答え：** **27, C, G**

**サルでもわかる解説：**  
- **print(int_a ** 3)** → 27 を表示。  
- **print((int_a**3) / int_b)** → 27/0 で **ZeroDivisionError**。  
- **except ZeroDivisionError** で **'C'** を表示。  
- **finally** で **'G'** を表示。  
- else は「例外が起きなかったとき」だけなので、ここでは実行されません。

---

### 問29：エラーと例外で**誤っている**ものはどれか

**答え：**  
「[Ctrl]+[C] キーなどでユーザーがプログラムに割り込みをかけると、**KeyError** 例外が送出される」が誤り。

**サルでもわかる解説：**  
- キーボード割り込みで送出されるのは **KeyboardInterrupt** です。  
- **KeyError** は「辞書に存在しないキーを参照したとき」の例外。  
- 名前が似ているので混同しないように。

---

### 問30：with open のクリーンアップ

**答え：** **f.close()**

**サルでもわかる解説：**  
- **with open(...) as f:** を抜けるときに、**ファイルオブジェクト f の close()** が自動で呼ばれます。  
- だから「適切なクリーンアップ」＝**f.close()** が正解。  
- **close('file.txt')** や **file.close()**（変数名は f）は誤り。

---

### 問31：例外の流れと【A】【B】【C】【D】

**答え：** **【A】strategic　【B】is a　【C】naughty boy　【D】AI**

**サルでもわかる解説：**  
1. **func(name)** で name='David'。0 でないので **raise_his_character('David')** が呼ばれる。  
2. **raise_his_character** で **print('David', '【B】')** → 【B】= **is a** で「David is a」と表示。  
3. 続いて **raise wexal** で **wexal** が発生。**print('【C】')** は raise の後なので**実行されない**（「naughty boy」は出ない）。  
4. **except wexal** で **print('【A】')** → 【A】= **strategic**。  
5. そのあと **raise Exception** で、外側の **except Exception** で **print('【D】')** → 【D】= **AI**。  
- 出力順は「David is a」→「strategic」→「AI」です。

---

### 問32：local / nonlocal / global のスコープ

**答え：** **A: init　B: nonlocal　C: global　D: global**

**サルでもわかる解説：**  
- **do_local()** の **loc = "local"** は**関数内の新しい変数**なので、外の **loc** は変わらない → **A: init**。  
- **do_nonlocal()** の **nonlocal loc** は「**ひとつ外の scope の loc**」を書き換える → **B: nonlocal**。  
- **do_global()** の **global loc** は「**モジュールの loc**」を "global" に変える → **C: global**。  
- 最後の **print("D:", loc)** は**モジュールの loc** を参照 → **D: global**。

---

### 問33：クラス継承の【A】【B】【C】

**答え：** **【A】self.m()　【B】m(self):　【C】s()**

**サルでもわかる解説：**  
- **kusanagi** の **s()** で「Need Speed?」を表示したあと、**self.m()** で自分の **m()** を呼ぶ。  
- **wexal** は **kusanagi** を継承し、**m(self):** をオーバーライドして「I'm David.」を表示。  
- **w.s()** は親の **s()** が呼ばれ、「Need Speed?」→**self.m()** で **wexal** の **m()** →「I'm David.」  
- だから 【A】= **self.m()**、【B】= **m(self):**、【C】= **s()** が正解。

---

## 8. その他

### 問34：sys.argv で ['script.py', 'one', 'two'] を得る

**答え：** **sys.argv[0:3]**

**サルでもわかる解説：**  
- **python3 script.py one two three four five** のとき、  
  **sys.argv = ['script.py', 'one', 'two', 'three', 'four', 'five']**（先頭に python3 があるかは実装依存。多くの場合 [0]=script.py, [1]=one, …）  
- 問題では [0] が 'script.py' とあるので、**argv[0:3]** で [0], [1], [2] = script.py, one, two が得られます。  
- **sys.args** は存在しないので ×。

---

### 問35：正規表現で ret[0] がエラーになるもの

**答え：** **ret = prog.search('kurofune')** など、**マッチしない**文字列を渡した場合。

**サルでもわかる解説：**  
- **re.search** が**マッチしない**と **None** を返す。  
- **None[0]** は **TypeError**（または AttributeError）になる。  
- 正規表現 `(K|S)u(r|s)(a|o)nf?...` に合わない文字列を選べばよい。  
- 「KUSANAGI」はマッチするのでエラーにならない。「kurofune」はパターンと合わないので **ret** が None になり、**ret[0]** でエラーになります。

---

### 問36：random モジュールの【A】【B】【C】

**答え：** **【A】choice　【B】sample　【C】randrange**

**サルでもわかる解説：**  
- **random.choice(リスト)** → 要素を**1つ**ランダムに返す。  
- **random.sample(範囲, 個数)** → 重複なしで**複数個**取り出す。  
- **random.randrange(終わり)** → 0 以上終わり未満の**整数**を1つ返す。  
- だから 【A】= choice、【B】= sample、【C】= randrange が正解。

---

### 問37：今日の日付を 2020-06-27 形式で得る

**答え：** **【A】from datetime import date　【B】date.today()**

**サルでもわかる解説：**  
- **from datetime import date** で **date** クラスだけをインポート。  
- **date.today()** で今日の日付オブジェクトを得て、**print(now)** で **YYYY-MM-DD** 形式で表示される。  
- **import datetime** の場合は **datetime.date.today()** と書く。

---

### 問38：logging の優先度（左から低い順）

**答え：** **DEBUG、INFO、WARNING、ERROR、CRITICAL**

**サルでもわかる解説：**  
- 数字が小さいほど「軽い」、大きいほど「重い」です。  
- この順で**左から右に優先度が高く**なります。

---

### 問39：pip で**誤っている**ものはどれか

**答え：**  
「**pip list パッケージ名**で、ある特定のパッケージの詳細情報が表示される」が誤り。

**サルでもわかる解説：**  
- **pip list** は「インストール済みパッケージの一覧」で、**引数にパッケージ名は取らない**。  
- 特定パッケージの**詳細**を見るのは **pip show パッケージ名**。  
- pip install == でバージョン指定、--upgrade、uninstall、freeze の説明は正しいです。

---

### 問40：対話環境などで**誤っている**ものはどれか

**答え：**  
「変数とモジュールの補完機能は、インタープリタの**起動時には有効になっていない**ため**設定が必要**である」が誤り。

**サルでもわかる解説：**  
- 標準の対話型インタープリタでも、**Tab 補完**（変数・モジュール名など）は**特に設定しなくても使える**ことが多い。  
- 「起動時には有効になっていない」「設定が必要」という説明が誤り。  
- .python_history、Tab の補完、bpython、IPython の説明は正しいです。

---

## まとめチェックリスト

| 分野           | 押さえるポイント |
|----------------|------------------|
| 特徴・インタープリタ | Perlより広い / sys.argv / listitems は存在しない |
| 数値・文字列     | 演算子とオペランド / // と % / 変数は未定義だとエラー / raw は r / 連結は + |
| リスト・スライス  | スライスは範囲外でもエラーにならない / 文字列はイミュータブル |
| 制御構文        | range の終わりは含まない / for-else は break しなかったときだけ |
| 関数           | デフォルト引数にリストは危険 / *args と **kwargs の順序 |
| モジュール      | sys.path にデフォルトは含まれる / dir(モジュール) で名前確認 |
| 例外           | [Ctrl]+[C] → KeyboardInterrupt / with のクリーンアップは f.close() |
| スコープ       | local / nonlocal / global の違い |
| クラス         | self.m() でメソッド呼び出し / オーバーライド |
| 標準ライブラリ  | random.choice / sample / randrange / datetime.date.today() / logging の優先度 |
| pip           | 詳細は pip show / pip list にパッケージ名は付けない |
| 対話環境       | 補完は設定なしでも有効 |

---

*このまとめは second.md の内容を整理し、初心者向けに解説したものです。実際の試験では公式の解説・正答を優先してください。*
