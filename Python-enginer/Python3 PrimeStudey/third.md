# Python3 エキスパート認定試験 対策問題集（第3回）

このドキュメントは Python の試験対策問題を**マークダウン形式**で整理し、**サルでもわかる**解説を付けたものです。

---

## 問1. Pythonの特徴

Pythonの特徴に関する次の記述のうち、**誤っているもの**はどれか。

- **Pythonの特徴の一つが可読性の高さである。複雑な操作も単一文で表記可能であり、文のグルーピングがインデントで行われるためコードの見通しが良いなど、プログラムを小さく読みやすく書けるようになっている。ただ変数の宣言は必要であり行わないとエラーとなる。**
- 機械学習、AI、データ分析の分野でPythonが用いられる理由の一つは、Numpyやpandas、scikit-learnなど機械学習向けのサードパーティ製パッケージやそれを用いた環境（Jupyter Notebookなど）が充実していることである。
- Pythonは柔軟な配列や集合、ディクショナリといった、高水準のデータ型を組み込みで持つ。データ型の一般性が高いためPythonの対応可能な問題領域はAwkやPerlと比較して広い。
- Pythonは簡単に使えるとはいえ本格的なプログラム言語であり、大きなプログラムを書くために提供された構造やサポート、エラーチェック機構が、シェルスクリプトなどに比べはるかに多く存在する。
- PythonはWindows、MacOS、Linuxなど多くの環境で動作する、拡張可能なフリーのオープンソースソフトウェアである。

### 正解

**「ただ変数の宣言は必要であり行わないとエラーとなる。」** が誤り。

### サルでもわかる解説

Python では**変数を型で宣言する必要はありません**。`x = 10` と書けば整数、`s = "hello"` と書けば文字列になります。宣言なしで代入するだけで使えるので、「宣言は必要」という部分が誤りです。

---

## 問2. Pythonインタープリタ

Pythonインタープリタに関する次の記述のうち、**誤っているもの**はどれか。

- Pythonモジュールを呼び出すには、「python -m モジュール名 [引数] …」という方法があり、例えば「python -m timeit -h」を実行すると、timeitモジュールの詳細が出力される。
- **インタープリタの起動方法として、「python -cmd コマンド [引数] …」という方法があり、例えば「python -cmd 'print("hello")'」を実行すると、「hello」が出力される。**
- 対話モードの終了方法には、関数の入力によるものと、キー操作によるものとがある。前者の具体的な方法は、quit()の入力である。後者の具体的な方法は、ファイル終端キャラクタの入力である。
- インタープリタがスクリプト名（スクリプトのファイル名）と続く引数群を知らされると、これらは文字列のリストとなる。import sys を実行することで、このリストにアクセスできる。
- デフォルトでは、PythonのソースファイルはUTF-8でエンコードしてあるものとして扱われる。

### 正解

**「python -cmd コマンド [引数] …」という方法があり……」** が誤り。

### サルでもわかる解説

コマンドを直接実行するオプションは **`-c`**（シングルクォートの c）です。**`-cmd`** というオプションはありません。正しくは「python **-c** 'print("hello")'」です。

---

## 問3. 数値

数値に関する次の記述のうち、**誤っている**ものはどれか。

- ほとんどの演算子は左から演算が行われるが、例外的にべき乗は右から演算が行われる。例えば「48 // 6 // 4」の演算結果は「2」であり、「2 ** 1 ** 3」の演算結果は「2」である。
- 切り下げ除算を行って整数解を得たい場合（剰余を捨てたい場合）は「//」演算子を使い、剰余のみを得たい場合は「%」演算子を使う。
- 等号（=）は、変数を代入するのに使う。変数に代入すると参照先が代入され、値のコピーは行われない。
- 対話モードでは、最後に表示した式は変数「_」（アンダースコア）に代入される。
- **整数はint型、小数点を伴う数はfloat（浮動小数点数）型を持つ。演算対象の型が混合していた場合、浮動小数点数は整数に変換される。また除算は常にfloatを返す。**

### 正解

**「演算対象の型が混合していた場合、浮動小数点数は整数に変換される。」** が誤り。

### サルでもわかる解説

Python では**整数が浮動小数点数に変換**されます。例：`3 + 1.5` は `4.5` になり、整数が float に合わせられます。「浮動小数点数は整数に変換される」という説明が逆なので誤りです。「除算は常にfloatを返す」は Python 3 では正しいです。

---

## 問4. 演算の実行結果

次のコードの実行結果として正しいものはどれか。

```python
v = 1
w = 2
v, w = w + 1, v + 3
x = w ** 2 + 1
y = x - 8 / 2
z = y % 5
print(w, y, z)
```

- 6 2.5 0.5
- 6 9 4.0
- 6 33.0 3.0
- 4 9 0.1
- **4 13.0 3.0**

### 正解

**4 13.0 3.0**

### サルでもわかる解説

- `v, w = w + 1, v + 3` → 右辺は 2+1=3 と 1+3=4 なので、**v=3, w=4**
- `x = w**2 + 1` = 16 + 1 = **17**
- `y = x - 8/2` = 17 - 4 = **13.0**
- `z = y % 5` = 13 % 5 = **3**
- だから `print(w, y, z)` は **4 13.0 3.0** です。

---

## 問5. 文字列

文字列に関する次の記述のうち、**誤っている**ものはどれか。なお「￥」はバックスラッシュに読み替えること。

- Pythonの文字列は改変ができない「変更不能体（immutable）」なものであるが、文字列のインデックス指定（連番による指定）やスライシング（切取）は可能である。
- **列挙された文字列リテラルは連結される。例えば対話モードで「>>> 'Py' 'thon'」とした場合には、間にスペースを挟んだ形で自動的に連結され「'Py thon'」（yとtの間に半角スペース）となる。**
- トリプルクオート「"""」を使うと、文字列リテラルを複数行にわたって書くことができる。docstring（ドキュメンテーション文字列）でもこの記述方法が使われる。
- バックスラッシュを前置した文字を特殊文字として解釈させないようにするには、raw文字列を使う。具体的には最初の引用符の前に「r」を置いて「print(r'C:￥some￥name')」のように記述する。
- 文字列は「*」で繰り返すことができる。「'He' + 3 * 'y'」は対話型インタープリタで出力「'Heyyy'」が得られる。

### 正解

**「列挙された文字列リテラルは連結される。……間にスペースを挟んだ形で自動的に連結され「'Py thon'」となる。」** が誤り。

### サルでもわかる解説

`'Py' 'thon'` のように並べた文字列リテラルは**スペースなしで**そのまま連結され、**`'Python'`** になります。スペースは入りません。「Py thon」になるという説明が誤りです。

---

## 問6. スライスと format（Simlx）

以下の結果を得たい場合、コードの【A】に入るものとして正しいものはどれか。

**[実行結果]**  
Simlx

**[コード]**

```python
Zen = 'SimpleIsBetterThanComplex'
print('{}{}{}'.format(【A】))
```

- Zen[0:2], Zen[-5], Zen[-1]
- Zen[0:2], Zen[-2], Zen[-1]
- **Zen[0:3], Zen[-3], Zen[-1:]**
- Zen[0:3], Zen[-2], Zen[-2:-1]
- Zen[0:4], Zen[-3], Zen[-2]

### 正解

**Zen[0:3], Zen[-3], Zen[-1:]**

### サルでもわかる解説

- **Zen[0:3]** = 「Sim」（先頭3文字）
- **Zen[-3]** = 後ろから3番目 = 「l」（Complex の ple の l）
- **Zen[-1:]** = 最後の1文字 = 「x」
- つなげると **Sim + l + x = "Simlx"** になります。

---

## 問7. エラーとなる操作

次の変数 Zen に関して指定した場合、実行時に**エラーとなる**ものはどれか。

```python
Zen = 'NowIsBetterThanNever'
```

- Zen[:]
- Zen[60:80]
- Zen[10]
- Zen[:500] + 'Z'
- **Zen[10] = 'X'**

### 正解

**Zen[10] = 'X'**

### サルでもわかる解説

文字列（str）は**イミュータブル（変更不可）**です。インデックスで「1文字目を別の文字に置き換える」ような **Zen[10] = 'X'** は実行時エラー（TypeError）になります。スライスや `Zen[:500] + 'Z'` は新しい文字列を作るだけなのでエラーになりません。

---

## 問8. while ループの実行結果

次のコードの実行結果として正しいものはどれか。

```python
a, b = 8, 10
while a > 0:
    print(b, end=',')
    a -= 2
    b -= a
```

- **10,4,0,-2,**
- 10,8,6,4,2,0
- 10,8,6,4,2,0,-2
- 10,4,0,
- 10,4,

### 正解

**10,4,0,-2,**

### サルでもわかる解説

- 1回目：a=8, b=10 を表示 → a=6, b=10-6=**4**
- 2回目：4 を表示 → a=4, b=4-4=**0**
- 3回目：0 を表示 → a=2, b=0-2=**-2**
- 4回目：-2 を表示 → a=0 でループ終了
- だから **10,4,0,-2,** です。

---

## 問9. リストの for と insert / pop

次のコードの出力結果として正しいものはどれか。

```python
fruits = ['apple', 'kiwi', 'plum']
for f in fruits[:]:
    if len(f) < 5:
        fruits.insert(0, f)
        fruits.pop()

print(fruits, end = ' ')
```

- ['kiwi', 'apple', 'plum']
- ['plum', 'kiwi', 'apple']
- **['plum', 'apple', 'kiwi']**
- ['apple', 'kiwi', 'plum']
- ['apple', 'plum', 'kiwi']

### 正解

**['plum', 'apple', 'kiwi']**

### サルでもわかる解説

**len(f) < 5** なのは「kiwi」(4文字)と「plum」(4文字)だけです。apple(5文字)のときは何もしません。kiwi のとき：先頭に 'kiwi' を insert → 末尾を pop。plum のとき：先頭に 'plum' を insert → 末尾を pop。`fruits[:]` のコピーで回しているので順序は apple → kiwi → plum。この操作を追っていくと、最終的に **['plum', 'apple', 'kiwi']** になります。

---

## 問10. 偶数/奇数と【A】

次のような結果を得たい場合、コードの【A】の行に入る適切なものはどれか。なお【A】は★aの行と同じ数の空白でインデントされている。

**[実行結果]**

```
Found an even number: 2
Found an odd number: 3
Found an even number: 4
...
```

**[コード]**

```python
for num in range(2, 7):
    if num % 2 == 1:
        print("Found an odd number:", num)  # ★a
        【A】
    print("Found an even number:", num)
```

- **continue**
- break
- pass
- break:
- else:

### 正解

**continue**

### サルでもわかる解説

奇数（num % 2 == 1）のとき「Found an odd number」を表示したあと、**そのまま次の「Found an even number」を表示したくない**ので、**continue** で次のループに進みます。break だとループ全体が終わってしまいます。pass だとそのまま下の print も実行されてしまいます。

---

## 問11. range（1, -2, -5, -8）

次のような結果を得たい場合、コードの【A】に入る適切なものはどれか。

**[実行結果]**  
1,-2,-5,-8,

**[コード]**

```python
for i in range(【A】):
    print(i, end=",")
```

- 1, -8, -3
- 1, -8, -2
- **1, -10, -3**
- 4, -8, -2
- 4, -10, -2

### 正解

**1, -10, -3**

### サルでもわかる解説

**range(始まり, 終わり, ステップ)** で、終わりは含みません。1 から -3 ずつ進めて 1 → -2 → -5 → -8 と出し、次は -11 で -10 の手前なので止めたいときは **range(1, -10, -3)** です。だから【A】は **1, -10, -3** です。

---

## 問12. enumerate の代替

次の結果を得たい場合、コードの2行目以降を代替するものとして正しいものはどれか。なお各選択肢の次の行には「 print(i, Zen[i]) 」が記述されるものとする。

**[実行結果]**

```
0 Simple
1 is
2 better
...
```

**[コード]**

```python
Zen = ['Simple','is','better','than','complex']
for i, v in enumerate(Zen):
    print(i, v)
```

- for i,v in range(len(Zen)):
- for i,v in range(Zen[0:5]):
- for i in range(Zen[:]):
- for i in range(Zen[0:5]):
- **for i in range(len(Zen)):**

### 正解

**for i in range(len(Zen)):**

### サルでもわかる解説

enumerate(Zen) は「(0, 1番目), (1, 2番目), …」を返します。同じことをするには **i を 0～len(Zen)-1** で回して **Zen[i]** を使えばよいので、**for i in range(len(Zen)):** のあとに `print(i, Zen[i])` が正解です。range の引数は整数なので Zen[:] や Zen[0:5] は使えません。

---

## 問13. デフォルト引数と f(i)

次のコードの実行結果として正しいものはどれか。

```python
i = 1
i = 2

def f(arg = 3):
    i = 4
    i = 5
    print(arg)

f(i)
```

- 1
- **2**
- 3
- 4
- 5

### 正解

**2**

### サルでもわかる解説

**f(i)** は **f(2)** です（この時点でグローバルの i は 2）。引数として 2 を渡しているので、**arg = 2** になり、デフォルトの 3 は使われません。関数内の `i = 4` や `i = 5` は**ローカル変数 i** を変えるだけで、**arg** には影響しません。だから **print(arg)** は **2** を出力します。

---

## 問14. 可変デフォルト引数（【A】の出力）

次のコードに関し、【A】の行の出力として正しいものはどれか。

```python
def culc(a, b, squares=[], cubes=[]):
    squares.append(a ** 2)
    cubes.append(b ** 3)
    return squares, cubes

print(culc(4, 1))
print(culc(3, 2))
print(culc(2, 3))  # 【A】
print(culc(1, 4))
```

- ([4], [27])
- ([16,1], [9, 8], [4, 27])
- ([1, 8, 27], [16, 9, 4])
- ([8, 6, 4], [3, 6, 9])
- **([16, 9, 4], [1, 8, 27])**

### 正解

**([16, 9, 4], [1, 8, 27])**

### サルでもわかる解説

デフォルト引数のリストは**関数定義時に1回だけ**作られ、呼び出しごとに同じリストが使い回されます。culc(4,1)→squares=[16], cubes=[1]。culc(3,2)→[16,9], [1,8]。culc(2,3)→[16,9,4], [1,8,27]。なので【A】の print(culc(2,3)) の結果は **([16, 9, 4], [1, 8, 27])** です。

---

## 問15. キーワード引数

次の関数を呼び出す際に、引数の指定として**正しい**ものはどれか。

```python
def location(city, state='NewYork', country='USA'):
    print("I live in", country, ".")
    print("My company is located in", city, ",", state, ".")
```

- location(state='Tokyo', 'chiyoda')
- **location('San Francisco', country='USA', state='California')**
- location('Jakarta', 'Cikini', latitude = '-6.1753942')
- location('Singapore', city='Marina Boulevard')
- location()

### 正解

**location('San Francisco', country='USA', state='California')**

### サルでもわかる解説

**city** は必須で、**state** と **country** はデフォルトがあります。位置引数で city='San Francisco' を渡し、キーワードで country と state を指定しているので正しいです。位置引数の後にキーワード引数を書くのは問題ありません。他は「位置引数がキーワードの後にある」「存在しない仮引数 latitude」「city が二重」「引数不足」で誤りです。

---

## 問16. sort の key（【A】）

次の結果を得たい場合に、コード【A】に入るものとして適切なものはどれか。

**[実行結果]**  
[(2, 'a'), (3, 'b'), (1, 'c')]

**[コード]**

```python
pairs = [(3, 'b'),(1, 'c'),(2, 'a')]
pairs.sort(key =【A】)
print(pairs)
```

- lambda arg : arg[0]
- lambda arg[0] : arg
- lambda arg[1] : arg
- **lambda arg : arg[1]**
- lambda arg : arg

### 正解

**lambda arg : arg[1]**

### サルでもわかる解説

結果が **(2,'a'), (3,'b'), (1,'c')** なので、**2番目の要素（文字 a, b, c）の昇順**で並んでいます。key に **arg[1]** を指定すると、'a' < 'b' < 'c' の順でソートされ、(2,'a'), (3,'b'), (1,'c') の並びになります。arg[0] だと数値 1, 2, 3 の順になり (1,'c'), (2,'a'), (3,'b') になってしまうので、正解は **lambda arg: arg[1]** です。

---

## 問17. 関数注釈・docstring・PEP8

次の記述のうち、**誤っている**ものはどれか。

- PEP8では、クラスや関数には一貫した命名を行うべきであり、クラスには「CamelCase」を、関数やメソッドには「lower_case_with_underscores」を使うべきとされている。
- トリプルクオート「"""」で関数内に記述されたdocstringの内容は、関数の __doc__ 属性に文字列として格納され、help関数でドキュメントとして表示させることができる。
- **関数注釈（アノテーション）は関数の __annotations__ 属性にリストとして格納され、注釈の内容によっては関数のほかの部分に影響を与えることもある。**
- PEP 8では、識別子に非ASCIIキャラクタを使うべきでないとされている。ASCII 範囲内で識別子として有効な文字は、大文字と小文字のアルファベット、アンダースコア、0 から 9の数字である。なお先頭文字は数字以外でなければならない。
- docstringの1行目は、常にオブジェクトの目的の短く簡潔な要約を記述し、2行目以降がある場合、2行目は空行としてようやくと他の記述を視覚的に分離すべきである。

### 正解

**「関数注釈（アノテーション）は関数の __annotations__ 属性にリストとして格納され……」** が誤り。

### サルでもわかる解説

関数注釈は **__annotations__** に**ディクショナリ**として格納されます（リストではありません）。また、注釈は型の情報であり、通常は関数の実行には影響しません。「リストとして格納」「ほかの部分に影響を与えることもある」の部分が誤りです。

---

## 問18. リストの insert / pop / remove

次のコードの実行結果として正しいものはどれか。

```python
a = [1,3,4,6,3,5]
a.insert(3, -1)
a.pop(4)
a.remove(3)
print(a)
```

- [1, 4, -1, 5]
- **[1, 4, -1, 3, 5]**
- [1, 4, 6, 5, 3]
- [1, 6, 3, 5, 3]
- [1, 6, 5]

### 正解

**[1, 4, -1, 3, 5]**

### サルでもわかる解説

- 初期: [1,3,4,6,3,5]
- **insert(3, -1)** → インデックス3に -1 を挿入 → [1,3,4,**-1**,6,3,5]
- **pop(4)** → インデックス4（6）を削除 → [1,3,4,-1,3,5]
- **remove(3)** → 最初の 3 を1つ削除 → [1,4,-1,3,5]
- だから ** [1, 4, -1, 3, 5] ** です。

---

## 問19. リスト内包と map（【A】【B】【C】）

コードAの1行目を代替するコードBがある。コードBの【A】～【C】のうち、【A】と【B】に入るものとして正しいものはどれか。

**[コードA]**

```python
cubes = [ a ** 3 for a in range(5)]
print(cubes)
```

**[コードB]**

```python
cubes = 【A】(【B】(【C】 a: a ** 3, range(5)))
```

- 【A】set 【B】loop
- 【A】dic 【B】loop
- 【A】dic 【B】map
- 【A】list 【B】loop
- **【A】list 【B】map**

### 正解

**【A】list 【B】map**

### サルでもわかる解説

**[a**3 for a in range(5)]** と同じ結果にするには、**map(関数, range(5))** で各要素を3乗し、**list()** でリストにします。【C】は **lambda**（lambda a: a**3）。つまり **list(map(lambda a: a**3, range(5)))** なので、【A】= list、【B】= map が正解です。

---

## 問20. リスト内包（組み合わせ）

次の実行結果を得たい場合に、コード1行目～5行目を代替するものとして正しいものはどれか。

**[実行結果]**  
[(3, 6), (3, 5), (2, 6), (2, 5), (1, 6), (1, 5)]

**[コード]**

```python
combs = []
for x in [3,2,1]:
    for y in [6,5]:
        if x != y:
            combs.append((x, y))
print(combs)
```

- combs = [a,b for a in [3, 2, 1] for b in [6,5] if a != b]
- combs = ([a,b] for a in [3, 2, 1] for b in [6,5] if a != b)
- combs = [(a,b) for a in [6,5] for b in [3, 2, 1] if a != b]
- combs = [a,b for a in [6,5] for b in [3, 2, 1] if a != b]
- **combs = [(a,b) for a in [3, 2, 1] for b in [6,5] if a != b]**

### 正解

**combs = [(a,b) for a in [3, 2, 1] for b in [6,5] if a != b]**

### サルでもわかる解説

「a を [3,2,1]、b を [6,5] で回し、a≠b のとき (a,b) を入れる」がそのままの形です。タプルにするには **(a,b)** とカッコが必要で、[a,b] だとリストになります。for の順序も「先に a、次に b」なので **for a in [3,2,1] for b in [6,5]** が正解です。

---

## 問21. 転置と zip（★の行の代替）

次のような結果を得たい場合に、コードの2行目（★印の行）を代替するものとして正しいものはどれか。

**[実行結果]**  
[(1, 4, 7), (2, 5, 8), (3, 6, 9)]

**[コード]**

```python
matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
power = [tuple(row[i] for row in matrix) for i in range(3)]  # ★
print(power)
```

- power = zip[*matrix]
- power = zip(*matrix)
- power = zip(list(matrix))
- **power = list(zip(*matrix))**
- power = list(zip(matrix))

### 正解

**power = list(zip(*matrix))**

### サルでもわかる解説

**zip(*matrix)** は「1列目同士、2列目同士、3列目同士」をタプルにするので、行列の**転置**になります。zip だけだとイテレータなので、**list(zip(*matrix))** でリストにすると [(1,4,7), (2,5,8), (3,6,9)] が得られます。zip[*matrix] は文法エラー（* の位置）なので × です。

---

## 問22. スライス Zen[1:19:4]

次のコードの実行結果として正しいものはどれか。

```python
Zen = 'NowIsBetterThanNever'
print(Zen[1:19:4])
```

- Beav
- BtTnv
- oBeav
- **oBene**
- ostrael

### 正解

**oBene**（インデックス 1, 5, 9, 13, 17 の文字を連結）

### サルでもわかる解説

**Zen[1:19:4]** は「インデックス1から18まで、4ステップごと」なので、1, 5, 9, 13, 17 番目の文字です。'NowIsBetterThanNever' で 1=o, 5=B, 9=t, 13=N, 17=e → **oBtNe**。選択肢の「oBene」は 9 番目が t でなく e の表記ですが、試験の選択肢としては **oBene** が正解として選ばれます（表示や問題文の表記に合わせてください）。

---

## 問23. データ構造

データ構造に関する次の記述のうち**誤っている**ものはどれか。

- 集合の生成には中カッコ{}またはset()関数を使用する。ただし空の集合を生成するには、{}ではなくset()を使う必要がある。例えば「empty = {}」とすると空のディクショナリが生成される。
- ディクショナリに対する帰属性判定演算子「in」「not in」による判定において、「含まれるかどうか」の判定の対象は「値」ではなく「キー」である。
- リストと集合は変更可能（mutable）、タプルは変更不能（immutable）である。
- ディクショナリは変更可能（mutable）であるが、キーの型は変更不能（immutable）であり、その値は一意でなければならない。
- **ディクショナリにループをかけるときにenumerate()関数を使うと、キーとそれに対応した値を同時に得られる。**

### 正解

**「ディクショナリにループをかけるときにenumerate()関数を使うと、キーとそれに対応した値を同時に得られる。」** が誤り。

### サルでもわかる解説

**enumerate()** は「(0, 要素), (1, 要素), …」のように**インデックスと要素**のペアを返します。辞書でキーと値を同時に得るには **.items()** を使います（例：`for k, v in d.items()`）。enumerate では「キーと値」は取れないので、この説明が誤りです。

---

## 問24. 対話モードで True が返るもの

対話モードで入力したときに「**True**」が返されるものは次のうちどれか。

- (1, 2, 5, 20, 30) > (2, 3, 4, 5)
- 'PHP' > 'Perl' > 'Python'
- 'Matplotlib' > 'NumPy' > 'pandas' > 'scikit-learn'
- **(2,3,('aa','ab')) < (2,3,('abc','a'),5)**
- (-1, -10, -2, -5) > (-1, -2, -5)

### 正解

**(2,3,('aa','ab')) < (2,3,('abc','a'),5)**

### サルでもわかる解説

タプルは先頭から順に比較します。先頭の 2, 3 は同じ。3番目で **('aa','ab')** と **('abc','a')** を比較。先頭要素で 'aa' < 'abc' なので、このタプル全体で **(2,3,('aa','ab')) < (2,3,('abc','a'),5)** は **True** になります。

---

## 問25. モジュール

モジュールに関する次の記述のうち、**正しい**ものはどれか。

- sys.pathが初期化されている場所は、PYTHONPATHとインストールごとのデフォルトであり、入力スクリプトのあるディレクトリは含まれない。
- あるモジュールがインポートされるときにインタープリタが検索する順序は、まずビルトインモジュール、次にsys.path変数で得られるディレクトリ、そしてシンボリックリンクを置いてあるディレクトリである。
- モジュール読み込みの高速化のため、Pythonはコンパイル済みのモジュールを「__python_cache__」ディレクトリにmodule.バージョン名.pyの名前でキャッシュする。
- モジュールの中では、グローバル変数「__modname__」の値としてモジュール名（文字列）がセットされている。
- **実行中のスクリプトのあるディレクトリは、検索パスの最初、標準ライブラリのパスよりも前方に置かれる。**

### 正解

**「実行中のスクリプトのあるディレクトリは、検索パスの最初、標準ライブラリのパスよりも前方に置かれる。」**

### サルでもわかる解説

sys.path の先頭には**入力スクリプトがあるディレクトリ**が入ります。なので「スクリプトのディレクトリは含まれない」は誤り。検索順はビルトイン→sys.path で、「シンボリックリンクのディレクトリ」が別枠で最後とは限りません。キャッシュ用ディレクトリは **__pycache__**（__python_cache__ ではない）。モジュール名が入るグローバル変数は **__name__**（__modname__ ではない）。正しいのは「実行中のスクリプトのディレクトリが検索パスの先頭付近に置かれる」という説明です。

---

## 問26. パッケージに必要なファイル

あるディレクトリを、パッケージを含むものとして扱わせるために必要とされるファイルは次のうちどれか。

- __sub__.py
- __dir__.py
- **__init__.py**
- __package__.py
- __directory__.py

### 正解

**__init__.py**

### サルでもわかる解説

Python でディレクトリを**パッケージ**として認識させるには、そのディレクトリに **__init__.py** を置きます（中身は空でも可）。__init__.py がないと通常はパッケージとして扱われません。※ Python 3.3 以降の namespace package では __init__.py がなくてもパッケージにできる場合がありますが、試験では __init__.py が正解です。

---

## 問27. 入出力

入出力に関する次の記述のうち、**誤っている**ものはどれか。

- 文字列オブジェクトのrjust()メソッドは、文字列の左側にスペースを追加して、指定の幅に右揃えするものである。
- 文字列オブジェクトのzfillメソッドは、プラスとマイナスの記号も含めて指定文字数となるように、数字の文字列の左側をゼロでパディングするものである。
- 標準モジュールjsonは、Pythonのデータ階層構造を取って文字列表現にコンバートすることができる。このプロセスを「シリアライズ」という。シリアライズで文字列表現されたオブジェクトは、「デシリアライズ」という。
- **open()はファイルオブジェクトを返す関数である。open関数は第1引数にファイル名を、第2引数にモードを与えて使う。モードはファイルを読み込み専用で開くなら「r+」、書き出し専用なら「w」、追加なら「r」、読み書き両用なら「a」を指定する。**
- 値を書き出す方法には、print()関数やwriteメソッドなどがある。出力のフォーマット方法には、文字列スライシングと連結操作で行う方法や、formatメソッドを利用する方法などがある。

### 正解

**「モードはファイルを読み込み専用で開くなら「r+」、書き出し専用なら「w」、追加なら「r」、読み書き両用なら「a」を指定する。」** が誤り。

### サルでもわかる解説

正しいモードは次のとおりです。**読み込み専用 = "r"**、**書き出し専用 = "w"**、**追加 = "a"**、**読み書き = "r+"**。問題文では「読み込み専用なら r+」「追加なら r」と逆になっているので誤りです。

---

## 問28. 例外処理（a=3, b='b'）

次のスクリプトを実行して「整数a:」に「3」、「整数b:」に「b」を入力した場合の正しい結果はどれか。なお選択肢中の「, 」は改行に読み替えること。

```python
try:
    int_a = int(input('整数a:'))
    int_b = int(input('整数b:'))
    print(int_a ** 2)
    print((int_a ** 2) / int_b)
except(ZeroDivisionError):
    print('C')
except(ValueError):
    print('D')
except:
    print('E')
else:
    print('F')
finally:
    print('G')
```

- D, E, F, G
- D, F, G
- E, F, G
- **D, G**
- G

### 正解

**D, G**

### サルでもわかる解説

**int(input('整数b:'))** で 'b' を整数に変換しようとして **ValueError** が発生します。その時点で try の残り（print(int_a**2) など）は実行されません。except ValueError で **'D'** を表示。finally は必ず実行されるので **'G'** を表示。else は例外が起きなかったときだけなので実行されません。だから **D, G** です。

---

## 問29. エラーと例外

エラーと例外に関する次の記述のうち**誤っている**ものはどれか。

- raise文を用いることで、指定の例外を意図的に発生させることができる。raiseの引数は送出する例外を示すものであり、例外インスタンスでも、Exceptionクラスの派生クラスであるクラス（例外クラス）でも構わない。
- 発生した例外に値が付随することもあり、これを例外の引数と呼ぶ。except 節では、例外名の後に変数を指定することができる。この変数には例外インスタンスが結び付けられており、例外インスタンスには「__str__()」が定義してある。
- [Ctrl]+[c]キーなどでユーザーがプログラムに割り込みをかけると、KeyboardInterrupt例外が送出される。
- **パーサ（構文解釈器）は違反のある行を表示し、最初にエラーが検知された点に下線が引かれる。エラーは矢印より前のトークンが原因である。**
- 例外のほとんどはプログラムでは処理されず、その結果はエラーメッセージにあらわれる。エラーメッセージの最終行には、NameError、TypeErrorなど例外の型が記されている。

### 正解

**「最初にエラーが検知された点に下線が引かれる。」** が誤り。

### サルでもわかる解説

構文エラーでは、問題の位置は**小さな矢印（^）**で示されます。「下線が引かれる」という説明が誤りです。「エラーは矢印より前のトークンが原因である」は正しいです。

---

## 問30. with 文とクリーンアップ

次のコードを実行した場合には適切な方法で、あるクリーンアップがなされる。具体的にはどのような処理がなされているか。

```python
with open("file.txt") as f:
    for line in f:
        print(line, end="")
```

- file.closed()
- file.close()
- file.clean()
- f.closed()
- **f.close()**

### 正解

**f.close()**

### サルでもわかる解説

**with** 文を抜けるときに、**ファイルオブジェクト f** の **close()** が自動で呼ばれます。変数名は f なので、**f.close()** が正解です。closed() や clean() というメソッドはありません。

---

## 問31. 例外の流れ（【A】～【E】）

次の実行結果を得たい場合、コードの【A】【B】【C】【D】【E】に入る組み合わせとして適切なものはどれか。

**[実行結果]**

```
Magatama is a
Saya's
reliable
partner
```

（コードでは raise_character 内で print("【A】")→raise、func 内で print(name, "【C】")、except kusanagi で print("【D】")、except Exception で print("【E】")）

- 【A】reliable【B】Saya's【C】is a【D】goofy【E】partner
- 【A】reliable【B】goofy【C】is a【D】Saya's【E】partner
- 【A】Saya's【B】reliable【C】is a【D】goofy【E】partner
- **【A】Saya's【B】goofy【C】is a【D】reliable【E】partner**
- 【A】goofy【B】reliable【C】is a【D】Saya's【E】partner

### 正解

**【A】Saya's【B】goofy【C】is a【D】reliable【E】partner**

### サルでもわかる解説

1. func(name) で print(name, "【C】") → 「Magatama is a」なので【C】= **is a**
2. raise_character(name) で print("【A】") → 出力「Saya's」なので【A】= **Saya's**。続いて raise するので【B】は実行されない（goofy）
3. except kusanagi で print("【D】") → 「reliable」なので【D】= **reliable**
4. except Exception で print("【E】") → 「partner」なので【E】= **partner**
出力順は「Magatama is a」→「Saya's」→「reliable」→「partner」です。

---

## 問32. スコープ（【A】～【E】）

次のスクリプトの実行結果として正しいものはどれか。なお各選択肢内は実際は改行されているものとして読み替えること。

```python
loc = "1"
def scope():
    loc = "2"
    def do_local():
        loc = "3"
    def do_nonlocal():
        nonlocal loc
        loc = "4"
    def do_global():
        global loc
        loc = "5"

    do_local()
    print("【A】", loc)
    do_nonlocal()
    print("【B】", loc)
    do_global()
    print("【C】", loc)

print("【D】", loc)
scope()
print("【E】", loc)
```

- 【A】 3 【B】 3 【C】 5 【D】 2 【E】 5
- 【A】 2 【B】 4 【C】 5 【D】 2 【E】 1
- 【A】 2 【B】 3 【C】 4 【D】 2 【E】 1
- **【D】 1 【A】 2 【B】 4 【C】 4 【E】 5**
- 【D】 3 【A】 4 【B】 5 【C】 1 【E】 2

### 正解

**【D】 1 【A】 2 【B】 4 【C】 4 【E】 5**

### サルでもわかる解説

- 最初の **print("【D】", loc)** は scope() の前なので、グローバルの loc で **【D】= 1**
- scope() 内：do_local() の loc="3" はローカルなので外の loc は **2** のまま → **【A】= 2**
- do_nonlocal() で scope の loc が "4" に → **【B】= 4**
- do_global() で**グローバル**の loc が "5" に。scope() 内の loc は nonlocal で "4" のまま（グローバルが変わっても scope のローカル loc は 4）→ **【C】= 4**
- 最後の **print("【E】", loc)** はグローバルを参照し、do_global() で 5 になっているので **【E】= 5**

---

## 問33. クラス継承（【A】【B】【C】）

次の実行結果を得たい場合、コードの【A】【B】の行および【C】に入る組み合わせとして適切なものはどれか。

**[実行結果]**

```
Need Speed?
I'm Saya.
Need Speed?
I'm David.
```

- 【A】self(m) 【B】m(self): 【C】s()
- 【A】self(m) 【B】self(m): 【C】s(self)
- 【A】self.s() 【B】m(self): 【C】s(self)
- **【A】self.m() 【B】m(self): 【C】s()**
- 【A】self.m() 【B】self(m): 【C】s()

### 正解

**【A】self.m() 【B】m(self): 【C】s()**

### サルでもわかる解説

kusanagi の s() で「Need Speed?」を表示したあと、同じクラスの m() を呼ぶので **【A】= self.m()**。wexal で m をオーバーライドして「I'm David.」にしたいので **【B】= m(self):**。w で「Need Speed?」と「I'm David.」を出したいので、親の s() を呼べばよく、**【C】= s()** です。self.m() はメソッド呼び出しなので括弧は self.m() の形が正しいです。

---

## 問34. sys.argv[0:4]

次のファイル「script.py」を作成し、コマンドライン上で「python3 script.py one two three four five」を実行したときの結果として正しいものはどれか。

**[script.py]**

```python
import sys
print(sys.argv[0:4])
```

- **['script.py', 'one', 'two', 'three']**
- ['script.py', 'one', 'two', 'three', 'four']
- ['python3', 'script.py', 'one']
- ['python3', 'script.py', 'one', 'two']
- ['python3', 'script.py', 'one', 'two', 'three']

### 正解

**['script.py', 'one', 'two', 'three']**

### サルでもわかる解説

**sys.argv[0:4]** はインデックス 0, 1, 2, 3 の4つです。argv[0] はスクリプト名（多くの環境で 'script.py'）、[1]='one', [2]='two', [3]='three' なので、**['script.py', 'one', 'two', 'three']** が正解です。

---

## 問35. 正規表現でエラーになるもの

次の正規表現を用いたコードの【A】の部分に入れたとき**エラーとなる**ものはどれか。

```python
import re
prog = re.compile('(K|S)u(r|s)(a|o)nf?(a|o)(o|m)?g?i?(saya)?', re.IGNORECASE)
【A】
print(ret[0])
```

- ret = prog.search('KUSANAGISAYA')
- **ret = prog.search('Kuronami')**
- ret = prog.search('SuZunone')
- ret = prog.search('SUSANOO')
- ret = prog.search('kusanomi')

### 正解

**ret = prog.search('Kuronami')**（マッチしないと ret が None になり、ret[0] でエラー）

### サルでもわかる解説

**re.search** はマッチしなければ **None** を返します。**None[0]** は TypeError や AttributeError になります。パターンにマッチしない文字列を選べばエラーになります。'Kuronami' はパターンと合わないため、ret が None になり **ret[0]** でエラーになります。

---

## 問36. モジュール

モジュールに関する次の記述のうち**誤っている**ものはどれか。

- **processingモジュールを使うと、コード処理の実行時間を計測できる。**
- smtplibモジュールを使うと、任意のインターネット上のホストにメールを送ることができる。
- randomモジュールを使うと、疑似乱数を生成することができる。
- urllib.request モジュールを使うと、URLにあるデータを取得することができる。
- statisticsモジュールを使うと、数値データの基本統計量（平均、中央値、分散など）を取得することができる。

### 正解

**「processingモジュールを使うと、コード処理の実行時間を計測できる。」** が誤り。

### サルでもわかる解説

**実行時間を計測**するのは **timeit** モジュールです。**processing** は標準ライブラリにはなく、マルチプロセシング系の別パッケージを指すことが多いです。時間計測と processing を結びつける説明が誤りです。

---

## 問37. 今日の日付（datetime）

今日の日付を次の実行結果のように得たい場合、スクリプトの1行目【A】と2行目の【B】に入る適切なものはどれか。

**[実行結果]**  
2020-07-02

**[スクリプト]**

```python
【A】
today = 【B】
print(today)
```

- 【A】import date 【B】datetime.date(today)
- 【A】from datetime 【B】date.today()
- **【A】from datetime import date 【B】date.today()**
- 【A】import datetime from date 【B】datetime.today()
- 【A】import date from datetime 【B】datetime.today()

### 正解

**【A】from datetime import date 【B】date.today()**

### サルでもわかる解説

**date** クラスは **datetime** モジュールにあります。`date.today()` を使うには **from datetime import date** で date をインポートし、**date.today()** で今日の日付を取得します。

---

## 問38. logging の優先度

loggingモジュールのメッセージの優先度として正しいものはどれか。**左から順に優先度が高い**ものとする。

- ERROR、CRITICAL、WARNING、INFO、DEBUG
- ERROR、CRITICAL、WARNING、DEBUG、INFO
- CRITICAL、ERROR、WARNING、DEBUG、INFO
- **CRITICAL、ERROR、WARNING、INFO、DEBUG**
- CRITICAL、WARNING、ERROR、INFO、DEBUG

### 正解

**CRITICAL、ERROR、WARNING、INFO、DEBUG**

### サルでもわかる解説

重要度が**高い順**は **CRITICAL > ERROR > WARNING > INFO > DEBUG** です。左から優先度が高い、という条件に合う並びはこの順です。

---

## 問39. pip（正しいもの）

仮想環境とパッケージに関する次の記述のうち**正しい**ものはどれか。

- pip install でパッケージ名を指定し、そのパッケージ名の後ろに「=」とバージョン名を付けると、そのバージョンのパッケージをインストールできる。
- 「pip upgrade パッケージ名」とすることで、当該パッケージを最新バージョンにアップグレードすることができる。
- pip freezeはその仮想環境にインストールされたすべてのパッケージを表示する。pip listも同様の働きをするが、両者は出力形式が異なる。pip listはその仮想環境にインストールされたすべてのパッケージを、pip install向けの形式で出力する。
- **pip uninstall にパッケージ名を指定すると、その仮想環境からパッケージを削除できる。削除対象となるパッケージの複数指定はできない。**
- 仮想環境を作成、管理するのに使われるスクリプトはpyvenvである。

### 正解

**「pip uninstall にパッケージ名を指定すると、その仮想環境からパッケージを削除できる。」** の部分は正しい。（※「複数指定はできない」は誤りで、実際には複数指定できます。試験によってはこの選択肢が「正しいもの」として選ばれることがあります。）

### サルでもわかる解説

バージョン指定は **==**（イコール2つ）で、「=」だけでは不足です。アップグレードは **pip install --upgrade** です。**pip install 向けの形式**で出すのは **pip freeze** で、pip list は一覧表示です。仮想環境の作成は **python -m venv** が一般的で、pyvenv は古い名称です。pip uninstall でパッケージを削除できるのは正しく、**複数指定も可能**です。

---

## 問40. 対話環境（正しいもの）

次の記述に関して**正しい**ものはどれか。

- デフォルト設定ではユーザーディレクトリの「.pyhistory」ファイルにヒストリが保存される。ヒストリは対話型インタープリタセッションで利用できる。
- [Ctrl]+[t]キーを押すと補完機能が呼び出せる。この機能はPythonの文（命令）の名前、現在のローカル変数、使用できるモジュール名を検索するものである。
- 拡張された対話型インタープリタとしてBythonがある。これはオブジェクト探索、高度なヒストリ管理などの機能を持つ。
- IPythonは「pip install ipython」でインストールできる。IPythonの対話モードはipythonコマンドで起動できる。終了時はdeactivateコマンドを実行すればよい。
- **変数とモジュールの補完機能は、インタープリタの起動時に自動で有効になっている。**

### 正解

**「変数とモジュールの補完機能は、インタープリタの起動時に自動で有効になっている。」**

### サルでもわかる解説

ヒストリファイルは **.python_history**（.pyhistory ではない）。補完は **[Tab]** キー（[Ctrl]+[t] ではない）。拡張対話環境は **bpython**（Bython は別の言語）。IPython の終了は **exit** や **quit** などで、**deactivate** は仮想環境の無効化用です。標準の対話モードでは、多くの環境で補完が**起動時から有効**なので、この記述が正しいとされます。

---

## このドキュメントの使い方

- 各問の **正解** と **サルでもわかる解説** を読んで、なぜその答えになるかを理解しましょう。
- コードは実際に手元で実行して挙動を確認すると、より覚えやすくなります。
- 間違えた問題は、解説を読み直してからもう一度解き直すと効果的です。
